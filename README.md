<h1 align="center">Cube Viewer</h1>

This is a Swing program meant to draw a cube.
The cube can be moved around with the mouse.

Written in Kotlin.

Run with `./gradlew run`

Build an executable binary with `./gradlew assemble`

